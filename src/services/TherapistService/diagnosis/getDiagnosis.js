import { BASE_API_URL } from "../../../constants";

/** Get the diagnosis of a peer with the id of `peerId`
 * @param {String} peerId The id of the peer whose diagnosis we want to get
 * @returns {Object|null} Returns an object containing diagnosis information if a diagnosis was found, otherwise returns null
 */
const getDiagnosis = async peerId => {};

//* EXPORTS
export default getDiagnosis;
